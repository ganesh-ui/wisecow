apiVersion: v1
kind: Service
metadata:
  name: wisecow
  namespace: wisecow
spec:
  selector:
    app: wisecow
  ports:
    - port: 80
      targetPort: 4499
      protocol: TCP
      name: http
  type: ClusterIP
  
